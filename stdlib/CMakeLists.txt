
if(CMAKE_HOST_UNIX)
    file(GLOB_RECURSE STD_LIB_FILES ${CMAKE_CURRENT_LIST_DIR}/${HOST_OS}/*.dll)

    install(FILES ${STD_LIB_FILES} DESTINATION ${CMAKE_SOURCE_DIR}/Build/${HOST_OS}/${CMAKE_BUILD_TYPE}/SlayingDemons/bin)
endif()